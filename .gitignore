node_modules/
dist/
screenshots/
.DS_Store
*.log
build/icon.iconset/

# Credencial real de la Brave API, empaquetada en el .app distribuido (ver profiles.js) pero
# NUNCA en el repo público — se coloca a mano en la máquina que hace el build oficial.
brave-api-key.local.json

# Mismo criterio para la credencial real de DeepL (traductor) — ver profiles.js.
deepl-api-key.local.json
