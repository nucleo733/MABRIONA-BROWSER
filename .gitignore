node_modules/
dist/
screenshots/
.DS_Store
*.log
build/icon.iconset/

# Credencial real de la Brave API — nunca en el repo público. Ya no se empaqueta dentro del .app
# distribuido (Fase 21): MABRIONA Browser usa el proxy real `mabriona.com/api/browser-search` por
# default. Este archivo solo importa si alguien configura su PROPIA key (ver profiles.js).
brave-api-key.local.json
